// Generated by CoffeeScript 1.3.3
(function() {
  var CMinion, MAXROUTES, Minion, app, express, i, port, r, _i;

  express = require("express");

  CMinion = require("../../minion");

  Minion = new CMinion();

  app = express();

  port = process.env.PORT || 3000;

  MAXROUTES = process.env.MAXROUTES || 100000;

  for (i = _i = 0; 0 <= MAXROUTES ? _i <= MAXROUTES : _i >= MAXROUTES; i = 0 <= MAXROUTES ? ++_i : --_i) {
    r = "/" + i;
    app.get(r, function(req, res) {
      res.send("Hello World.");
      return Minion.logRequest();
    });
  }

  Minion.started();

  app.listen(port, function() {
    return console.log("Listening on port " + port);
  });

}).call(this);
