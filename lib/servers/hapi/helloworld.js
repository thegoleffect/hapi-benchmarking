// Generated by CoffeeScript 1.3.3
(function() {
  var CMinion, Hapi, Minion, dns, hello, host, os, port, server;

  dns = require("dns");

  os = require("os");

  Hapi = require("hapi");

  CMinion = require("../../minion");

  Minion = new CMinion();

  host = process.env.HOST || 'localhost';

  port = process.env.PORT || 3000;

  server = new Hapi.Server(host, port);

  hello = {
    method: "GET",
    path: "/",
    config: {
      handler: function(req) {
        req.reply("Hello World.");
        return Minion.logRequest();
      }
    }
  };

  server.addRoute(hello);

  Minion.started();

  server.start();

}).call(this);
